name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      
      - name: Install backend dependencies
        working-directory: ./backend
        run: |
          if [ -f "package-lock.json" ]; then
            npm ci
          else
            npm install
          fi
          echo "✅ Dependencias del backend instaladas"
      
      - name: Check database file exists
        run: |
          if [ -f "backend/database/apexremedy.db" ]; then
            echo "✅ Base de datos encontrada"
            ls -lh backend/database/apexremedy.db
          else
            echo "⚠️ Advertencia: Base de datos no encontrada en backend/database/apexremedy.db"
            echo "📋 Archivos en backend/database/:"
            ls -la backend/database/ || echo "Directorio no existe"
            echo "⚠️ Continuando sin exportar productos (JSON estático no se generará)"
          fi
      
      - name: Check existing JSON files
        run: |
          if [ -f "frontend/api/products.json" ]; then
            PRODUCT_COUNT=$(cat frontend/api/products.json | grep -o '"total":[0-9]*' | grep -o '[0-9]*' | head -1)
            if [ -n "$PRODUCT_COUNT" ] && [ "$PRODUCT_COUNT" -gt 0 ]; then
              echo "✅ JSON existente tiene $PRODUCT_COUNT productos"
              echo "PRODUCTS_EXIST=true" >> $GITHUB_ENV
            else
              echo "⚠️ JSON existente está vacío o no tiene productos"
              echo "PRODUCTS_EXIST=false" >> $GITHUB_ENV
            fi
          else
            echo "⚠️ JSON no existe"
            echo "PRODUCTS_EXIST=false" >> $GITHUB_ENV
          fi
      
      - name: Export products to JSON (Static API)
        working-directory: ./backend
        continue-on-error: true
        run: |
          # Si ya hay productos en el JSON, no hacer nada
          if [ "$PRODUCTS_EXIST" = "true" ]; then
            echo "✅ Manteniendo JSON existente con productos"
            exit 0
          fi
          
          if [ -f "../database/apexremedy.db" ] || [ -f "database/apexremedy.db" ]; then
            echo "🚀 Exportando productos a JSON estático..."
            DB_PATH=${DB_PATH:-database/apexremedy.db} node scripts/export-products-to-json.js
            echo "✅ Exportación completada"
          else
            echo "⚠️ Base de datos no encontrada y no hay JSON con productos"
            echo "⚠️ Creando JSON vacío para evitar errores..."
            mkdir -p ../frontend/api
            echo '{"success":true,"message":"JSON vacío - base de datos no disponible","data":{"products":[],"total":0,"timestamp":"'$(date -u +"%Y-%m-%dT%H:%M:%SZ")'"}}' > ../frontend/api/products.json
            echo '{"success":true,"message":"JSON vacío - base de datos no disponible","data":{"products":[],"total":0,"timestamp":"'$(date -u +"%Y-%m-%dT%H:%M:%SZ")'"}}' > ../frontend/api/products-featured.json
            echo "✅ JSON vacío creado"
          fi
      
      - name: Verify JSON files
        run: |
          if [ -f "frontend/api/products.json" ]; then
            echo "✅ products.json encontrado"
            echo "📊 Tamaño del archivo: $(du -h frontend/api/products.json | cut -f1)"
            PRODUCT_COUNT=$(cat frontend/api/products.json | grep -o '"total":[0-9]*' | grep -o '[0-9]*' | head -1)
            if [ -n "$PRODUCT_COUNT" ]; then
              echo "📦 Productos en JSON: $PRODUCT_COUNT"
            fi
            echo "📄 Primeras líneas:"
            head -n 5 frontend/api/products.json
          else
            echo "⚠️ Advertencia: products.json no encontrado, creando JSON vacío..."
            mkdir -p frontend/api
            echo '{"success":true,"message":"JSON vacío","data":{"products":[],"total":0,"timestamp":"'$(date -u +"%Y-%m-%dT%H:%M:%SZ")'"}}' > frontend/api/products.json
            echo '{"success":true,"message":"JSON vacío","data":{"products":[],"total":0,"timestamp":"'$(date -u +"%Y-%m-%dT%H:%M:%SZ")'"}}' > frontend/api/products-featured.json
            echo "✅ JSON vacío creado"
          fi
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './frontend'
          retention-days: 30
        env:
          GITHUB_PAGES_BASE_PATH: '/apexremedy.github.io'
      
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

